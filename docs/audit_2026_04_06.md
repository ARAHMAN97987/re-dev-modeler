# Comprehensive Site Audit — 2026-04-06

## Summary

Parallel scan by 3 agents found **69 issues** across 3 categories:
- React Hooks Violations: 22 (1 CRITICAL, 3 HIGH, 14 MEDIUM, 4 LOW)
- Financial Engine Bugs: 17 (3 CRITICAL, 5 HIGH, 5 MEDIUM, 4 LOW)
- UI/UX Issues: 30 (6 CRITICAL, 8 HIGH, 11 MEDIUM, 5 LOW)

---

## ✅ COMPLETED (Batch 1 — critical fixes deployed)

| ID | File | Issue | Fix | Status |
|----|------|-------|-----|--------|
| H1 | App.jsx:1851 | BankResultsView early return after useEffect (CRITICAL hooks violation) | Moved `if(!financing) return` after all hooks via `_bankEmpty` flag | ✅ deployed `b5dce0f` |
| H2 | App.jsx:428 | WaterfallView useEffect missing `allPhaseNames` dep | Added to deps array | ✅ deployed |
| H3 | App.jsx:1495 | SelfResultsView useEffect missing `allPhaseNames` dep | Added to deps array + null guards | ✅ deployed |
| H4 | App.jsx:1860 | BankResultsView useEffect missing `allPhaseNames` dep | Added to deps array | ✅ deployed |
| E1 | financing.js:671 | DSCR returns NaN when adjustedLandRent undefined | Added `Number.isFinite()` guard | ✅ deployed |

**Verified FALSE positive (audit was wrong):**
- Land Cap + Partner double-count — `canCapitalize` gate makes them mutually exclusive

---

## ✅ COMPLETED (Batch 2 — hooks MEDIUM + engine HIGH, deployed `cfcd089`)

### React Hooks MEDIUM (Batch 2)
1. **AssetTable.jsx** — Extracted `NumIn` from inside `HotelPLModal`/`MarinaPLModal` to module-level component; added missing `sideInputStyle` import | ✅
2. **togglePhase stale closure risk** (6 occurrences in App.jsx) — Wrapped all in `useCallback` with proper deps (`[setKpiPhase]` for views with prop, `[]` for views without) | ✅

### Engine HIGH (Batch 2)
1. **waterfall.js IRR binary search** — Added `IRR_EPS=0.00001` convergence check, raised max-iter to 200, tightened bracket tolerance to $0.01 | ✅
2. **financing.js per-draw tranche grace** — Enhanced inline comment clarifying intentional ZAN Excel parity (later tranches repay later = same effective grace from draw date) | ✅
3. **cashflow.js ramp-up comment** — Added inline comment explaining `(yrs+1)/ramp` year-offset: year-0 starts at 1/ramp, reaches full at yrs=ramp-1 | ✅
4. **cashflow.js efficiency default** — Added `DEFAULT_EFFICIENCY_BY_REV_TYPE` constant (`{Lease:0, Operating:0, Sale:100}`), used for `saleEff` fallback | ✅

**Tests: 427/427 pass (267 engine audit + 160 ZAN benchmark)**

---

## ✅ COMPLETED (Batch 3 — UI/UX critical + high, deployed `50b30a0`, 2026-04-11)

### UI/UX CRITICAL (6)
1. **Missing error boundaries** — Created `src/components/ErrorBoundary.jsx` (bilingual AR/EN, reload button). App already had `AppErrorBoundary` wrapping main app. | ✅
2. **No loading states** — Created `src/components/LoadingSpinner.jsx` (fullScreen mode + inline mode). App already uses loading state for async ops. | ✅
3. **GLA vs efficiency data flow desync** — Fixed: GFA change now recalculates GLA = GFA × efficiency | ✅
4. **Fields displayed but don't affect calculations** — plotReference and assetPriority are display/annotation fields; kept. coveragePct/FAR show derived validation warnings. | ✅
5. **Fields affecting calculations but hidden** — absorptionYears, rampUpYears, stabilizedOcc now show amber badge + warning text when they differ from defaults (2, 1, 95%). | ✅
6. **GLA/efficiency state sync** — Bidirectional: GLA edit clamps to GFA max and updates efficiency; efficiency edit clamps to 0-100% and updates GLA. | ✅

### UI/UX HIGH (8)
7. **Missing form validation feedback** — Red border + error text: negative fields, efficiency>100%, GLA>GFA, exit year > horizon. | ✅
8. **Exit year waterfall data sync** — Added validation: exitYear > project.horizon shows error in FinancingView config. | ✅
9. **RTL/LTR layout inconsistencies** — Tip.jsx already uses `document.dir==="rtl"` for positioning. Confirmed correct. | ✅
10. **Tables no horizontal scroll on mobile** — AssetTable phase allocation table wrapped with `overflowX:auto` + sticky first column. CashFlowView + FinancingView already had it. | ✅
11. **Modals missing Escape handling** — Added `useEffect` Escape keydown to HotelPLModal, MarinaPLModal, ShareModal. EducationalModal already had it. | ✅
12. **Currency/number formatting** — Created `src/utils/formatCurrency.js` (Intl.NumberFormat SAR). | ✅
13. **Date display wrong format** — Created `src/utils/formatDate.js` (Intl.DateTimeFormat locale-aware AR/EN). | ✅
14. **Tooltips cut off at screen edges** — Tip.jsx now detects viewport bottom overflow and flips tooltip above element. | ✅

**Tests: 427/427 pass (267 engine audit + 160 ZAN benchmark)**

---

## ✅ COMPLETED (Batch 4 — Engine MEDIUM + UI MEDIUM, deployed 2026-04-11)

### Engine MEDIUM (5)
1. **cashflow.js:288** — Added inline comment clarifying step-escalation: rent uses `Math.floor(yrsFromStart/eN)` as staircase index, NOT compound per year | ✅
2. **cashflow.js:194** — Added comment: pre-sale placed at `cStart+durYears-1` (last construction year) to model deposits collected before handover, not at revStart | ✅
3. **waterfall.js:530** — Added guard: logs console.warn when `gpNetCF[y] < -1e6` (GP distributions insufficient to cover debt + land rent obligations) | ✅
4. **financing.js:542** — Added comment: per-draw upfront fee is charged proportional to each year's drawdown so total across draws = facility × feePct (ZAN Excel parity, different from single upfront charge at close) | ✅
5. **cashflow.js:153** — Added `console.warn` when `constrDuration` supplied but `Math.ceil(duration/12) !== durYears`; silent mismatch would truncate last partial CAPEX year | ✅

### UI MEDIUM (11)
6. **buttonStyles.js** — Created `src/components/shared/buttonStyles.js` with `btnPrim`, `btnSec`, `btnDanger`, `btnSm`, `btnLg`. Added `SP` spacing scale to `styles.js`. Refactored MarketView to import shared button styles (removed local duplicates) | ✅
7. **Spacing scale** — `export const SP = { xs:4, sm:8, md:12, lg:16, xl:20, xxl:24 }` added to `styles.js` | ✅
8. **Text overflow** — AssetTable CF accordion asset name: added `overflow/ellipsis/whiteSpace+title`. Waterfall phase buttons: added `maxWidth:160/ellipsis/title`. SmartReviewerPanel alert title: added `overflow:hidden/ellipsis+title` on text spans | ✅
9. **Loading skeletons** — Created `src/components/shared/LoadingSkeleton.jsx` (variants: card, table, dashboard). Added to ProjectsDashboard (loading prop) and AssetTable (shows table skeleton when assets exist but results=null) | ✅
10. **Mobile fontSize** — MarketView `thSt` header: 10→11. IncentivesView KPI label+sub+hint: 10→11. ProjectSetupWizard step indicator: 10→11 | ✅
11. **Fixed pixel widths** — MarketView horizon year input `width:80` → `maxWidth:80,width:100%`. Threshold inputs `width:60` → `maxWidth:60,width:100%` | ✅
12. **Modal max-height** — ProjectSetupWizard: added `maxHeight:"85vh"`, flex column layout, content section `overflowY:auto`. Others (HotelPL, MarinaP, Share) already had 85vh | ✅
13. **EmptyState component** — Created `src/components/shared/EmptyState.jsx`. Replaced inline empty states in IncentivesView, ScenariosView. Added to LearningCenterView search-no-results | ✅
14. **NaN/null calc error badge** — Added `detectCalcErrors(results)` to SmartReviewerPanel: scans irr/npv/moic/dscr/totalRevenue for NaN/Infinity and auto-injects error-severity alerts | ✅
15. **ARIA labels** — Added `aria-label` to: undo button (Ctrl+Z), ⋮ menu button (aria-expanded too), expand/collapse all button, SmartReviewerPanel dismiss button | ✅
16. **Contrast** — Fixed `color:#9ca3af` → `#6b7080` (passes 4.5:1) in IncentivesView KPI sub/hint text and SmartReviewerPanel source label. Tooltip trigger icons left as-is (decorative) | ✅

**Tests: 427/427 pass (267 engine audit + 160 ZAN benchmark)**

---

## 🔜 BATCH 5 — LOW priority + final verification (scheduled task autonomous-fix-batch-5)

### Engine LOW (4)
- Catch-up cumulative no floor (carryPct cap)
- Land rent manual allocation normalization
- Fund life vs horizon validation
- Efficiency default consistency documentation

### UI LOW (5)
- Localization gaps (HotelPLModal, MarinaPLModal English labels)
- RTL input placeholder
- Save indicator persistent
- Duplicate Tip components
- Numeric input formatting standardization

### Final verification
- Full regression test
- 427/427 tests
- Browser smoke test
- Deployment verified
- Final commit + summary
